spring:
  datasource:
    url: jdbc:postgresql://localhost:8081/TRANSACOES
    driver-class-name: org.postgresql.Driver
    username: csv-batch
    password: csv-batch
  batch:
    initialize-schema: always

file:
  reader:
    key: fileReader
  name:
    key: fileName
  line:
    key: fileLine
  column:
    line: 1